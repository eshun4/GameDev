#include "raylib.h"

int main()
{
	// Function body
	int width;
	width = 350;
	// Call a function that initializes  window (takes multiple input)
	InitWindow(width, 200, "Stephen's Window");
	return 0;
}
